
set(PACKAGE_VERSION @BALAU_VERSION@)

if(NOT "${PACKAGE_FIND_VERSION_COUNT}" EQUAL 3)
	set(PACKAGE_VERSION_UNSUITABLE 1)
	set(PACKAGE_VERSION_COMPATIBLE 0)
elseif("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL @BALAU_VERSION_YEAR@)
	if("${PACKAGE_FIND_VERSION_MINOR}" EQUAL @BALAU_VERSION_MONTH@)
		set(PACKAGE_VERSION_COMPATIBLE 1)
		if("${PACKAGE_FIND_VERSION_PATCH}" EQUAL @BALAU_VERSION_PATCH@)
			set(PACKAGE_VERSION_EXACT 1)
		endif ()
	endif ()
endif ()

